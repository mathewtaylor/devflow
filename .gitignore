# Local DevFlow installation (for testing)
.claude
.devflow

# DevFlow runtime files (if .devflow/ is tracked in other projects)
*.bak
*.tmp

# Plugin testing guide (local development only)
PLUGIN_TESTING.md
